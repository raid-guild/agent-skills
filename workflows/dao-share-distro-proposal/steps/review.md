# Review

Human gate. Review evidence, distro table, proposal payload, DAO target, and Discord review summary.

Approval must identify whether the next step should submit onchain or remain build-only.

Route:

- `approved` -> `submit-onchain`
- `changesRequested` -> `build-distro`
- `rejected` -> `closed`
