# Build Distro

Use `rg-dao-ops`.

Build the distribution table from the approved evidence and rules.

Produce `share-distro-table.json` and `share-distro-summary.md` with recipients, units/shares/amounts, rationale, totals, and unresolved exceptions.
