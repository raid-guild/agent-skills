# Submit Onchain

Use `rg-dao-ops`.

This agent node has `autoRun: false`. Continue only after the operator approves the exact DAO, chain, proposal payload, wallet/action path, and submit intent.

Re-verify the exact approved parameters before preparing or submitting. If the run is build-only, record that no submission was made.

Capture proposal id, transaction hash, unsigned artifact, or failure details in `dao-submit-result.md`.
