# Queen Raida Social Graph Ops

This workflow handles Queen Raida account graph changes such as follows. It is separate from `queen-raida-social-publish` because following changes public account posture rather than content.

Discovery can run automatically after steering, but applying follows has `autoRun: false`.
