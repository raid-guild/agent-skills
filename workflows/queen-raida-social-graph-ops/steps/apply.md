# Apply Approved Changes

Use `rg-publishing-ops`.

This agent node has `autoRun: false`. Continue only after the operator approves exact handles/accounts.

Before applying, verify each handle resolves to the intended account and skip accounts already followed.

Apply only approved changes and produce `queen-raida-social-graph-result.md` with applied, skipped, failed, and evidence entries.
