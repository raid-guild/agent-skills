# Publish Wiki Page

Use `rg-portal-ops`.

This agent node has `autoRun: false`. Continue only after the operator approves exact page target, visibility, and payload.

Create or update the approved Portal wiki page(s). Re-read records after writing and capture URLs, ids, and failures.
