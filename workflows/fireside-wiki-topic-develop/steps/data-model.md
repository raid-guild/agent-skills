# Wiki Data Model

Use `rg-content-strategy` and `rg-portal-ops`.

Create `wiki-data-model.json` for the proposed page or page set, including title, slug candidate, summary, sections, source links, related pages, tags, and open questions.
