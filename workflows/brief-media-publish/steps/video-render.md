# Video Render

Use `rg-media-render-ops`.

Submit the approved `render-payload.json` to the configured renderer or prepare the exact reviewed render command for the operator.

Produce `render-job.md` with:

- composition
- payload reference
- job id or command
- expected output
- status URL or polling instructions, if available
- failure details if submission fails
