# Collect Data

Use `rg-portal-ops`.

Collect the requested Portal, feedback, activity, and arcade data for the approved time window.

Produce `portal-activity-data.json` and `portal-activity-source-notes.md` with counts, source endpoints, time window, skipped sources, and failures.
