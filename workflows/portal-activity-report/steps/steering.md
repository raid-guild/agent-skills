# Steering

Human gate. Confirm report scope:

- source mode: `weekly_activity`, `arcade`, `feedback`, or `ad_hoc`
- time window
- target audience and destination
- whether the report is internal-only or public-safe
- Discord channel or artifact-only output

Route approved requests to `collect-data`.
