# Publish Summary

Use `rg-publishing-ops`.

This agent node has `autoRun: false`. Continue only after the operator approves destination and final text.

Post or queue the approved summary to the approved destination, or attach it as a request artifact when artifact-only mode is selected.

Produce `portal-activity-publish-result.md` with destination, final text, posted URL/message id or artifact id, and failures.
