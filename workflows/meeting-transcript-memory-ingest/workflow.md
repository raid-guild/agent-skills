# Meeting Transcript Memory Ingest

This is a narrow human-assisted workflow for ingesting a meeting transcript into Prism Memory.

It depends on Prism built-in skills `change-request-ops` and `prism-api-writer`. This repo should not duplicate the Prism Memory API write contract.
