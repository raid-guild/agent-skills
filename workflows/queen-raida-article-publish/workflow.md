# Queen Raida Article Publish

This workflow creates one public Queen Raida website article in `https://github.com/raid-guild/Queen-Raida`.

It depends on Prism site built-in `target-deploy-ops` for repo work, branch management, validation, deploy checks, and PR workflow.
