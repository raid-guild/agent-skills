Use x-developer-api, queen-raida-social-agent, and public-social-output-sanitizer. Before publishing, run public-social-output-sanitizer one final time on the exact approved text. If the sanitizer returns any high or critical blocker, do not publish and return to human review. Publish only the human-approved sanitizedText and only to the approved target action: reply, quote/reply if supported, or new post. Verify the X credentials resolve to @raidguildish unless the approved request explicitly names another verified target. Do not alter approved copy except for API-required formatting. After publishing, capture the posted X id and URL. Write a Prism memory inbox item with source queen-raida-x-response, type x_response_published, bucket_hint social, content containing the approved sanitizedText, source item URL, posted URL, sanitizer changes/blockers, and request/workflow reference if available. Attach or record the posted URL as an artifact/external ref through change-request-ops when a request context is available.
