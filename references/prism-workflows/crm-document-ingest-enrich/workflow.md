# CRM Document Ingest Enrich

Handle document upload events sent from NextCRM. This workflow does not require a target repository, does not modify code, does not call /admin/*, and completes after the intake step saves the required artifacts.
